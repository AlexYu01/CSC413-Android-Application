package com.example.team33.groupfinder;

import android.content.Context;

import com.example.team33.groupfinder.model.Group;

import java.util.ArrayList;
import java.util.List;
import java.util.UUID;

/*public class Groups {
    private static Group sGroups;

    private ArrayList<Group> mStorylist;

    private Groups(Context context) {
        mStorylist = new ArrayList<>();
    }

    public static Groups get(Context context) {
        if (sGroups == null) {
            sGroups = new Groups(context);
        }
        return sGroups;
    }

    public List<Group> getStoryList() {
        return mStorylist;
    }


//TODO mod this later
    public Group getStory(UUID id) {
        for (Group story : mStorylist) {
            if (story.getGroupId().equals(id)) {
                return story;
            }
        }
        return null;
    }
} */
